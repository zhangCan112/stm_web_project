
const prefix = '/v1'

/// 用户信息
const USER =  prefix + '/user/'
///登录
const LOGIN = prefix + '/user/login'
///注册
const LOGOUT = prefix + '/user/logout'


const URLS = {
    USER,
    LOGIN,
    LOGOUT,
}

export default URLS